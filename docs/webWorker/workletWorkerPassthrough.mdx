---
title: AudioWorklet WebWorker Passthrough
sidebar_position: 2
---
<head>
  <title>coi-serviceworker</title>
  <script src="coi-serviceworker.js"></script>
  <meta name="Cross-Origin-Opener-Policy" content="same-origin" />
  <meta name="Cross-Origin-Embedder-Policy" content="require-corp" />
</head>

import BrowserOnly from "@docusaurus/BrowserOnly";
import WorkletWorker from "../../src/components/WorkletWorker";

export default function WebGpuWorker() {
  console.log("test", self.crossOriginIsolated);
  return (
    <BrowserOnly fallback={<div>Loading...</div>}>
      {() => <WorkletWorker />}
    </BrowserOnly>
  )
}