---
title: WebGPU Audio WebWorker Stream
sidebar_position: 3
---

import BrowserOnly from "@docusaurus/BrowserOnly";
import WebGpuWorkerStream from "../../src/components/WebGpuWorkerStream";

export default function WebGpuWorker() {
  return (
    <BrowserOnly fallback={<div>Loading...</div>}>
      {() => <WebGpuWorkerStream/>}
    </BrowserOnly>
  )
}