---
title: WebGPU Audio WebWorker Example (not streaming)
sidebar_position: 1
---

import BrowserOnly from "@docusaurus/BrowserOnly";
import WebGpuAudioWorker from "../../src/components/WebGpuAudioWorker";

export default function WebGpuWorker() {
  return (
    <BrowserOnly fallback={<div>Loading...</div>}>
      {() => <WebGpuAudioWorker/>}
    </BrowserOnly>
  )
}